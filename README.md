📌 LoreStack

LoreStack is a community-driven social web platform focused on mood-based content discovery and discussion.
The platform allows users to explore, share, and interact with content (posts, media, movies, series) in a structured and transparent way.

⚙️ Backend Setup
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python manage.py migrate
python manage.py runserver

⚙️ Frontend Setup
npm install
npm run dev


👥 Team

Aayush Sapkota — Backend Development

Nitesh — Frontend Development

Bimal — Frontend Development